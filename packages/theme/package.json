{
  "name": "@jupitermoney/io-theme",
  "type": "module",
  "version": "1.0.0",
  "description": "Tokens library for design system",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "compile": "tsc",
    "clear": "rm -rf dist",
    "generate": "ts-node ./scripts/generate.ts",
    "build": "yarn clear && yarn generate && yarn compile",
    "prepare": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "style-dictionary": "^3.9.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@shopify/restyle": "^2.4.2",
    "react": "18.2.0",
    "react-native": "0.72.6"
  }
}
